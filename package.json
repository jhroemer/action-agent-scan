{
  "name": "action-agent-scan",
  "version": "0.0.1",
  "description": "A GitHub action that helps detecting if a contributor is an AI agent.",
  "keywords": [
    "action",
    "github",
    "workflow",
    "ai",
    "agent"
  ],
  "license": "MIT",
  "author": "Jens Rømer Hesselbjerg",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "test": "vitest run",
    "format": "prettier --write src/index.ts",
    "build": "tsc --noEmit && ncc build src/index.ts"
  },
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^9.0.0",
    "voight-kampff-test": "0.0.1"
  },
  "devDependencies": {
    "@types/node": "25.2.3",
    "@vercel/ncc": "0.38.4",
    "prettier": "3.8.1",
    "typescript": "5.9.3",
    "vitest": "4.0.18"
  }
}
